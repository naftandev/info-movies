import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  carouselContainer: {
    height: 440,
  },
  categoriesContainer: {
    marginTop: 5,
    marginBottom: 25
  }
});

export default styles;
