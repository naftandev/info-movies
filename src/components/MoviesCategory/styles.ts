import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 260,
  },
  title: {
    marginBottom: 5,
    marginLeft: 7.5,
    fontSize: 30,
    fontWeight: 'bold'
  }
});

export default styles;
